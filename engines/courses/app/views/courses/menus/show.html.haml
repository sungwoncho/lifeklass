= render(@contents) || render('no_content')
.pagination= will_paginate @contents, renderer: ::BootstrapPagination::Rails

= link_to 'New content', courses_app.new_course_menu_content_path(current_course, current_menu)
