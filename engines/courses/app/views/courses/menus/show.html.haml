= render(@contents) || render('no_content')
= will_paginate @contents, renderer: ::BootstrapPagination::Rails

.action.mt30
  = link_to 'New content', course_app.new_course_menu_content_path(current_course, current_menu)
